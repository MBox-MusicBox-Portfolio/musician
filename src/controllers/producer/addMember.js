import db from '../../modules/db.js';
import Member from '../../models/member.js';
import {checkUserRole} from '../../middleware/isProducer.js';

export async function addMember(producer)
{
   if(producer)
   {
      //Member
   }
}